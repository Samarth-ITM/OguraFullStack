export { repositories } from "./mock";
import { repositories } from "./mock";

export const catalogRepository = repositories.catalog;
export const cartRepository = repositories.cart;
export const wishlistRepository = repositories.wishlist;
export const accountRepository = repositories.account;
export const checkoutRepository = repositories.checkout;
